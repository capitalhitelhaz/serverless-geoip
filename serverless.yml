service: serverless-geoip

provider:
  name: aws
  runtime: nodejs8.10
  memorySize: 256
  timeout: 30
  region: eu-west-1
  profile: ${self:provider.environment.AWS_PROFILE,''}

package:
  exclude:
    - .gitignore
    - CODE_OF_CONDUCT.md
    - dist/**
    - LICENSE.md
    - README.md
    - yarn.lock
    - package.json
    - package-lock.json

functions:
  lookup:
    handler: src/lookup.run
    events:
      - http:
          method: GET
          path: /ip/{ip}
          integration: lambda
          request:
            parameters:
              paths:
                ip: true
            template:
              application/json: '{ "ip" : "$input.params(''ip'')" }'
          response:
            headers:
              Content-Type: "'application/json'"
            template: $input.json('$')
  lookupDefault:
    handler: src/lookup.run
    events:
      - http:
          method: GET
          path: /ip
          integration: lambda
          response:
            headers:
              Content-Type: "'application/json'"
            template: $input.json('$')